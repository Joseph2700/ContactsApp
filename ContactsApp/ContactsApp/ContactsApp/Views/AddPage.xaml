<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ContactsApp.Views.AddPage"
             Title="New User">
    <ContentPage.Content>
        <StackLayout>
            <Entry x:Name="entryAddName" Placeholder="Name" Text ="{Binding ContactSelected.Name}"/>
            <Entry x:Name="entryAddNumber" Placeholder="Number" Text="{Binding ContactSelected.Number}"/>
            <Button Text="ADD" BackgroundColor="Black" TextColor="White" Command="{Binding AddContactCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>